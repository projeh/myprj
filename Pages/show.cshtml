@page
@using System.Data;

@model mhmdhidry.Pages.showModel
@{
}


<input class="btn btn-info btn-lg"  type="text"/>


  
<h2>Index</h2>  
  
<p>  
    
    <a  asp-action="Create">Create New</a>  
</p>  
<table cellpadding="0" cellspacing="0">
    <tr>
        <th>CustomerId</th>
        <th>Name</th>
        <th>****</th>
    </tr>
    @foreach (DataRow row in Model.ds_crs_reg.Tables[0].Rows)
    {
        <tr>
            <td>@row["CrgFullName"]</td>
            <td>@row["CrgDT"]</td>
            <td>
                <a  asp-page="/Index4">update</a>
                <a   asp-page="/Index4">update</a>
                <a asp-page="/index4" asp-route-id="@row["id"]">edit</a>

                <a  asp-page="/index5" asp-route-id="@row["id"]">edit</a>
                <a  asp-page="/index6" asp-route-id="@row["id"]">delete</a>

            </td>
        </tr>
    } 
   
</table>
<a  asp-page="/Index4">update</a>
